node_modules/
dist/
coverage/
.turbo/
.next/
out/
*.tsbuildinfo

.env
.env.local
.env.*.local
!.env.example

# Written by `next dev` on every run, and its own note asks to commit it.
# It is agent tooling, not part of the application, so it stays out of the
# repository -- see next/dist/server/lib/generate-agent-files.js.
apps/web/AGENTS.md
apps/web/CLAUDE.md

.pglite/
tmp/
*.log

.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Written by `vercel link`: the project id and a pulled .env.local.
.vercel
